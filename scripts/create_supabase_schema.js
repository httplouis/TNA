console.error('This script is not usable because Supabase requires a database connection string for direct Postgres access.');
console.error('Use the Supabase SQL editor or the repository migration tool instead.');
process.exit(1);

